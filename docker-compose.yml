services:
  bridgelayer:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - bridgelayer-data:/app/data
    environment:
      DATABASE_URL: sqlite:////app/data/bridgelayer.db

volumes:
  bridgelayer-data:
